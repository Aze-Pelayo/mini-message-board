extends layout

block content
  h1= title
  p Welcome to #{title}!

  a(href='/new') Add a message

  each message in messages
    div
      p #{message.text} #{message.user}
      p #{message.added}
    